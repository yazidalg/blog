<h1> <%= @article.title %> </h1>
<p> <%= @article.body %> </p>

<%= link_to "Back to Article", articles_path %>
<%= link_to "Edit Artikel", edit_article_path(@article) %>
<%= link_to "Delete", article_path(@article), data: {
    turbo_method: :delete,
    turbo_confirm: "Yakin dek?"
}%>
